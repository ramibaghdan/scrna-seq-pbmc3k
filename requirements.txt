# pip alternative to environment.yml (use in a venv):
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
scanpy
leidenalg
igraph
matplotlib
jupyterlab
jupytext
